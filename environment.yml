name: attrpred-env
channels:
  - pytorch
dependencies:
  - python=3.8
  - pip
  - build
  - ipykernel
  - numpy
  - matplotlib
  - pyyaml
  - pytorch
  - torchvision
  - cudatoolkit=11.3  # replace with 'cudatoolkit=11.3' if you have a gpu or 'cpuonly' if cpu
  - pandas
  - tensorboard
  - scikit-learn
  - apache-beam
  - tqdm
  - tabulate
  - pytest
  - pytest-cov
  - pytest-mock
  - mypy
  - pylint
  - black
  - isort
  - pip:
    - torch-tb-profiler